<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <%- include ('templates/head') %>
    <script type="text/javascript" src="/js/upload_picture.js"></script>
  </head>

  <body>
    <%- include ('templates/nav_manage') %>
    
    <div class="manage">

      <h1>編輯餐點</h1>

      <form method="POST" class="manage__edit" action="/manage/menu/edit/<%= menu.id %>">

        <div class="image">
          <div class="image__block">
            <img id="image" src="<%= menu.url %>" alt="">
          </div>
          <input class="image__url" hidden type="text" name="url" value="<%= menu.url %>" readonly />
          <input class="image__upload" type="file"/>
        </div>
        
        <div class="title">
          <h3>餐點名稱</h3>
          <input type="text" name="title" maxlength="20" value="<%= menu.title %>"/>
        </div>
        
        <div class="content">
          <h3>價格（請填數字）</h3>
          <input type="num" min="1" max="5000" name="price" value="<%= menu.price %>"/>
        </div>
        
        <div class="order">
          <h3>順序（請填數字）</h3>
          <p class="manage__explain">順序需在數字 1 ~ 99 之間</p>
          <input type="number" min="1" max="100" name="order" value="<%= menu.order %>"/>
        </div>

        <div class="remind">
          <% if (errorMessage[0]) { %> <%= errorMessage %> <% } %>
        </div>

        <div class="post__btn">
          <button type="submit" class="edit-post__btn">送出</button>
        </div>
      
      </form>

    </div>
  </body>
</html>
